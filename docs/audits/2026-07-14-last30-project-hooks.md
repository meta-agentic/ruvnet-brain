# The last 30 projects — every hook, read verbatim (2026-07-14)

Read-only. Every command string extracted in full; duplication PROVEN by exact-match dedup;
unparseable files listed raw below (count: 0).

## Per project

| # | project | last commit | hook cmds | npx-rUv | local-helper | dead | mcp-npx | other |
|---|---|---|---|---|---|---|---|---|
| 1 | ruvnet-brain | 2026-07-14 | 0 | 0 | 0 | 0 | 0 | 0 |
| 2 | u-go-far-linkedin-generator | 2026-07-13 | 11 | 5 | 4 | 0 | 0 | 2 |
| 3 | Ruv-Explainer | 2026-07-13 | 1 | 1 | 0 | 0 | 1 | 0 |
| 4 | All In Expert | 2026-07-13 | 17 | 1 | 16 | 0 | 1 | 0 |
| 5 | cognitum-learn | 2026-07-13 | 16 | 0 | 16 | 0 | 0 | 0 |
| 6 | openclaw-stack | 2026-07-12 | 0 | 0 | 0 | 0 | 0 | 0 |
| 7 | AppealArmor | 2026-07-09 | 11 | 3 | 2 | 3 | 1 | 3 |
| 8 | ugo-ai-register-now | 2026-07-09 | 11 | 5 | 0 | 0 | 0 | 6 |
| 9 | Synseer | 2026-07-09 | 0 | 0 | 0 | 0 | 0 | 0 |
| 10 | SpotOn | 2026-07-07 | 0 | 0 | 0 | 0 | 0 | 0 |
| 11 | PowerPlatePulse | 2026-07-02 | 24 | 1 | 15 | 0 | 1 | 8 |
| 12 | websitecopy | 2026-06-28 | 0 | 0 | 0 | 0 | 0 | 0 |
| 13 | Site_Master | 2026-06-28 | 8 | 5 | 0 | 1 | 0 | 2 |
| 14 | ISO Vision AI new Site | 2026-06-28 | 0 | 0 | 0 | 0 | 0 | 0 |
| 15 | Helix - Personal Health Intelligence Platform | 2026-06-26 | 0 | 0 | 0 | 0 | 0 | 0 |
| 16 | U-GO  Scholar | 2026-06-10 | 17 | 1 | 16 | 0 | 1 | 0 |
| 17 | bwe-counsel | 2026-06-08 | 0 | 0 | 0 | 0 | 0 | 0 |
| 18 | Mark T Seed | 2026-06-03 | 0 | 0 | 0 | 0 | 0 | 0 |
| 19 | cognitum-one-sensor-primer | 2026-05-30 | 0 | 0 | 0 | 0 | 0 | 0 |
| 20 | cognitum-learn-site | 2026-05-30 | 0 | 0 | 0 | 0 | 0 | 0 |
| 21 | Ugo-Ustay | 2026-04-24 | 0 | 0 | 0 | 0 | 0 | 0 |
| 22 | Paul_Sherrock | 2026-04-12 | 0 | 0 | 0 | 0 | 0 | 0 |
| 23 | BWEconstruction | 2026-04-07 | 17 | 1 | 16 | 0 | 1 | 0 |
| 24 | musica | 2026-04-07 | 0 | 0 | 0 | 0 | 0 | 0 |
| 25 | rvm | 2026-04-06 | 0 | 0 | 0 | 0 | 0 | 0 |
| 26 | rudevolution | 2026-04-03 | 0 | 0 | 0 | 0 | 0 | 0 |
| 27 | claude-internals-skill | 2026-03-31 | 1 | 0 | 0 | 0 | 0 | 1 |
| 28 | BizCard | 2026-03-24 | 0 | 0 | 0 | 0 | 0 | 0 |
| 29 | CMO-Focus-GTS | 2026-03-24 | 17 | 0 | 16 | 0 | 0 | 1 |
| 30 | PaperBanana | 2026-03-09 | 0 | 0 | 0 | 0 | 0 | 0 |

## EVERY DISTINCT COMMAND (90 unique strings across 151 occurrences)

Each listed IN FULL — this is the "read one by one", with duplication proven, not assumed.

---
**[local-helper] × 9** in: All In Expert, PowerPlatePulse, U-GO  Scholar

```
sh -c 'exec node "${CLAUDE_PROJECT_DIR:-.}/.claude/helpers/hook-handler.cjs" session-end'
```
---
**[local-helper] × 6** in: BWEconstruction, CMO-Focus-GTS

```
node .claude/helpers/hook-handler.cjs session-end
```
---
**[inline/other] × 3** in: u-go-far-linkedin-generator, AppealArmor, Site_Master

```
/bin/bash -c 'echo "🔄 Auto-Compact Guidance (Context Window Full):"; echo "📋 CRITICAL: Before compacting, ensure you understand:"; echo " • All 54 agents available in .claude/agents/ directory"; echo " • Concurrent execution patterns from CLAUDE.md"; echo " • Batchtools optimization for 300% performance gains"; echo " • Swarm coordination strategies for complex tasks"; echo "⚡ Apply GOLDEN RULE: Always batch operations in single messages"; echo "✅ Auto-compact proceeding with full agent context"'
```
---
**[local-helper] × 3** in: All In Expert, PowerPlatePulse, U-GO  Scholar

```
sh -c 'exec node "${CLAUDE_PROJECT_DIR:-.}/.claude/helpers/hook-handler.cjs" pre-bash'
```
---
**[local-helper] × 3** in: All In Expert, PowerPlatePulse, U-GO  Scholar

```
sh -c 'exec node "${CLAUDE_PROJECT_DIR:-.}/.claude/helpers/hook-handler.cjs" pre-edit'
```
---
**[local-helper] × 3** in: All In Expert, PowerPlatePulse, U-GO  Scholar

```
sh -c 'exec node "${CLAUDE_PROJECT_DIR:-.}/.claude/helpers/hook-handler.cjs" post-edit'
```
---
**[local-helper] × 3** in: All In Expert, PowerPlatePulse, U-GO  Scholar

```
sh -c 'exec node "${CLAUDE_PROJECT_DIR:-.}/.claude/helpers/hook-handler.cjs" post-bash'
```
---
**[local-helper] × 3** in: All In Expert, PowerPlatePulse, U-GO  Scholar

```
sh -c 'exec node "${CLAUDE_PROJECT_DIR:-.}/.claude/helpers/hook-handler.cjs" route'
```
---
**[local-helper] × 3** in: All In Expert, PowerPlatePulse, U-GO  Scholar

```
sh -c 'exec node "${CLAUDE_PROJECT_DIR:-.}/.claude/helpers/hook-handler.cjs" session-restore'
```
---
**[local-helper] × 3** in: All In Expert, PowerPlatePulse, U-GO  Scholar

```
sh -c 'exec node "${CLAUDE_PROJECT_DIR:-.}/.claude/helpers/hook-handler.cjs" compact-manual'
```
---
**[local-helper] × 3** in: All In Expert, PowerPlatePulse, U-GO  Scholar

```
sh -c 'exec node "${CLAUDE_PROJECT_DIR:-.}/.claude/helpers/hook-handler.cjs" compact-auto'
```
---
**[local-helper] × 3** in: All In Expert, PowerPlatePulse, U-GO  Scholar

```
sh -c 'exec node "${CLAUDE_PROJECT_DIR:-.}/.claude/helpers/hook-handler.cjs" status'
```
---
**[local-helper] × 3** in: All In Expert, PowerPlatePulse, U-GO  Scholar

```
sh -c 'exec node "${CLAUDE_PROJECT_DIR:-.}/.claude/helpers/hook-handler.cjs" post-task'
```
---
**[local-helper] × 3** in: All In Expert, PowerPlatePulse, U-GO  Scholar

```
sh -c 'exec node "${CLAUDE_PROJECT_DIR:-.}/.claude/helpers/hook-handler.cjs" notify'
```
---
**[npx:@claude-flow] × 3** in: All In Expert, U-GO  Scholar, BWEconstruction

```
npx -y @claude-flow/cli@latest mcp start
```
---
**[local-helper] × 3** in: cognitum-learn

```
sh -c 'D="${CLAUDE_PROJECT_DIR:-.}"; [ -f "$D/.claude/helpers/hook-handler.cjs" ] || D="${HOME}"; exec node "$D/.claude/helpers/hook-handler.cjs" session-end'
```
---
**[npx:claude-flow(old)] × 2** in: u-go-far-linkedin-generator, AppealArmor

```
cat | jq -r '.tool_input.file_path // .tool_input.path // empty' | tr '\n' '\0' | xargs -0 -I {} npx claude-flow@alpha hooks pre-edit --file '{}' --auto-assign-agents true --load-context true
```
---
**[npx:claude-flow(old)] × 2** in: u-go-far-linkedin-generator, AppealArmor

```
cat | jq -r '.tool_input.file_path // .tool_input.path // empty' | tr '\n' '\0' | xargs -0 -I {} npx claude-flow@alpha hooks post-edit --file '{}' --format true --update-memory true
```
---
**[npx:claude-flow(old)] × 2** in: u-go-far-linkedin-generator, ugo-ai-register-now

```
npx claude-flow@alpha hooks session-end --generate-summary true --persist-state true --export-metrics true
```
---
**[inline/other] × 2** in: u-go-far-linkedin-generator, Site_Master

```
/bin/bash -c 'INPUT=$(cat); CUSTOM=$(echo "$INPUT" | jq -r ".custom_instructions // """); echo "🔄 PreCompact Guidance:"; echo "📋 IMPORTANT: Review CLAUDE.md in project root for:"; echo " • 54 available agents and concurrent usage patterns"; echo " • Swarm coordination strategies (hierarchical, mesh, adaptive)"; echo " • SPARC methodology workflows with batchtools optimization"; echo " • Critical concurrent execution rules (GOLDEN RULE: 1 MESSAGE = ALL OPERATIONS)"; if [ -n "$CUSTOM" ]; then echo "🎯 Custom compact instructions: $CUSTOM"; fi; echo "✅ Ready for compact operation"'
```
---
**[local-helper] × 2** in: All In Expert, U-GO  Scholar

```
sh -c 'exec node "${CLAUDE_PROJECT_DIR:-.}/.claude/helpers/auto-memory-hook.mjs" import'
```
---
**[local-helper] × 2** in: All In Expert, U-GO  Scholar

```
sh -c 'exec node "${CLAUDE_PROJECT_DIR:-.}/.claude/helpers/auto-memory-hook.mjs" sync'
```
---
**[local-helper] × 2** in: BWEconstruction, CMO-Focus-GTS

```
node .claude/helpers/hook-handler.cjs pre-bash
```
---
**[local-helper] × 2** in: BWEconstruction, CMO-Focus-GTS

```
node .claude/helpers/hook-handler.cjs pre-edit
```
---
**[local-helper] × 2** in: BWEconstruction, CMO-Focus-GTS

```
node .claude/helpers/hook-handler.cjs post-edit
```
---
**[local-helper] × 2** in: BWEconstruction, CMO-Focus-GTS

```
node .claude/helpers/hook-handler.cjs post-bash
```
---
**[local-helper] × 2** in: BWEconstruction, CMO-Focus-GTS

```
node .claude/helpers/hook-handler.cjs route
```
---
**[local-helper] × 2** in: BWEconstruction, CMO-Focus-GTS

```
node .claude/helpers/hook-handler.cjs session-restore
```
---
**[local-helper] × 2** in: BWEconstruction, CMO-Focus-GTS

```
node .claude/helpers/auto-memory-hook.mjs import
```
---
**[local-helper] × 2** in: BWEconstruction, CMO-Focus-GTS

```
node .claude/helpers/auto-memory-hook.mjs sync
```
---
**[local-helper] × 2** in: BWEconstruction, CMO-Focus-GTS

```
node .claude/helpers/hook-handler.cjs compact-manual
```
---
**[local-helper] × 2** in: BWEconstruction, CMO-Focus-GTS

```
node .claude/helpers/hook-handler.cjs compact-auto
```
---
**[local-helper] × 2** in: BWEconstruction, CMO-Focus-GTS

```
node .claude/helpers/hook-handler.cjs status
```
---
**[local-helper] × 2** in: BWEconstruction, CMO-Focus-GTS

```
node .claude/helpers/hook-handler.cjs post-task
```
---
**[local-helper] × 2** in: BWEconstruction, CMO-Focus-GTS

```
node .claude/helpers/hook-handler.cjs notify
```
---
**[inline/other] × 2** in: claude-internals-skill, CMO-Focus-GTS

```

```
---
**[npx:claude-flow(old)] × 1** in: u-go-far-linkedin-generator

```
cat | jq -r '.tool_input.command // empty' | tr '\n' '\0' | xargs -0 -I {} npx claude-flow@alpha hooks pre-command --command '{}' --validate-safety true --prepare-resources true
```
---
**[local-helper] × 1** in: u-go-far-linkedin-generator

```
bash .claude/helpers/github-checkpoint-hooks.sh pre-edit "{{tool_input}}"
```
---
**[npx:claude-flow(old)] × 1** in: u-go-far-linkedin-generator

```
cat | jq -r '.tool_input.command // empty' | tr '\n' '\0' | xargs -0 -I {} npx claude-flow@alpha hooks post-command --command '{}' --track-metrics true --store-results true
```
---
**[local-helper] × 1** in: u-go-far-linkedin-generator

```
bash .claude/helpers/github-checkpoint-hooks.sh post-edit "{{tool_input}}"
```
---
**[local-helper] × 1** in: u-go-far-linkedin-generator

```
bash .claude/helpers/github-checkpoint-hooks.sh task "{{user_prompt}}"
```
---
**[local-helper] × 1** in: u-go-far-linkedin-generator

```
bash .claude/helpers/github-checkpoint-hooks.sh session-end
```
---
**[npx:ruflo] × 1** in: Ruv-Explainer

```
npx -y ruflo@latest mcp start
```
---
**[local-helper] × 1** in: cognitum-learn

```
sh -c 'D="${CLAUDE_PROJECT_DIR:-.}"; [ -f "$D/.claude/helpers/hook-handler.cjs" ] || D="${HOME}"; exec node "$D/.claude/helpers/hook-handler.cjs" pre-bash'
```
---
**[local-helper] × 1** in: cognitum-learn

```
sh -c 'D="${CLAUDE_PROJECT_DIR:-.}"; [ -f "$D/.claude/helpers/hook-handler.cjs" ] || D="${HOME}"; exec node "$D/.claude/helpers/hook-handler.cjs" pre-edit'
```
---
**[local-helper] × 1** in: cognitum-learn

```
sh -c 'D="${CLAUDE_PROJECT_DIR:-.}"; [ -f "$D/.claude/helpers/hook-handler.cjs" ] || D="${HOME}"; exec node "$D/.claude/helpers/hook-handler.cjs" post-edit'
```
---
**[local-helper] × 1** in: cognitum-learn

```
sh -c 'D="${CLAUDE_PROJECT_DIR:-.}"; [ -f "$D/.claude/helpers/hook-handler.cjs" ] || D="${HOME}"; exec node "$D/.claude/helpers/hook-handler.cjs" post-bash'
```
---
**[local-helper] × 1** in: cognitum-learn

```
sh -c 'D="${CLAUDE_PROJECT_DIR:-.}"; [ -f "$D/.claude/helpers/hook-handler.cjs" ] || D="${HOME}"; exec node "$D/.claude/helpers/hook-handler.cjs" route'
```
---
**[local-helper] × 1** in: cognitum-learn

```
sh -c 'D="${CLAUDE_PROJECT_DIR:-.}"; [ -f "$D/.claude/helpers/hook-handler.cjs" ] || D="${HOME}"; exec node "$D/.claude/helpers/hook-handler.cjs" session-restore'
```
---
**[local-helper] × 1** in: cognitum-learn

```
sh -c 'D="${CLAUDE_PROJECT_DIR:-.}"; [ -f "$D/.claude/helpers/auto-memory-hook.mjs" ] || D="${HOME}"; exec node "$D/.claude/helpers/auto-memory-hook.mjs" import'
```
---
**[local-helper] × 1** in: cognitum-learn

```
sh -c 'D="${CLAUDE_PROJECT_DIR:-.}"; [ -f "$D/.claude/helpers/auto-memory-hook.mjs" ] || D="${HOME}"; exec node "$D/.claude/helpers/auto-memory-hook.mjs" sync'
```
---
**[local-helper] × 1** in: cognitum-learn

```
sh -c 'D="${CLAUDE_PROJECT_DIR:-.}"; [ -f "$D/.claude/helpers/hook-handler.cjs" ] || D="${HOME}"; exec node "$D/.claude/helpers/hook-handler.cjs" compact-manual'
```
---
**[local-helper] × 1** in: cognitum-learn

```
sh -c 'D="${CLAUDE_PROJECT_DIR:-.}"; [ -f "$D/.claude/helpers/hook-handler.cjs" ] || D="${HOME}"; exec node "$D/.claude/helpers/hook-handler.cjs" compact-auto'
```
---
**[local-helper] × 1** in: cognitum-learn

```
sh -c 'D="${CLAUDE_PROJECT_DIR:-.}"; [ -f "$D/.claude/helpers/hook-handler.cjs" ] || D="${HOME}"; exec node "$D/.claude/helpers/hook-handler.cjs" status'
```
---
**[local-helper] × 1** in: cognitum-learn

```
sh -c 'D="${CLAUDE_PROJECT_DIR:-.}"; [ -f "$D/.claude/helpers/hook-handler.cjs" ] || D="${HOME}"; exec node "$D/.claude/helpers/hook-handler.cjs" post-task'
```
---
**[local-helper] × 1** in: cognitum-learn

```
sh -c 'D="${CLAUDE_PROJECT_DIR:-.}"; [ -f "$D/.claude/helpers/hook-handler.cjs" ] || D="${HOME}"; exec node "$D/.claude/helpers/hook-handler.cjs" notify'
```
---
**[script:DEAD] × 1** in: AppealArmor

```
"$CLAUDE_PROJECT_DIR"/.claude/hooks/UserPromptSubmit.sh
```
---
**[local-helper] × 1** in: AppealArmor

```
node .claude/helpers/brain-checkpoint.cjs verify --json
```
---
**[script:DEAD] × 1** in: AppealArmor

```
"$CLAUDE_PROJECT_DIR"/.claude/hooks/SessionStart.sh
```
---
**[local-helper] × 1** in: AppealArmor

```
node .claude/helpers/brain-checkpoint.cjs export --json
```
---
**[script:DEAD] × 1** in: AppealArmor

```
/bin/bash -c 'cd /Users/stuartkerr/Code/AppealArmor && node scripts/ruvector-health-monitor.js --quick 2>/dev/null || npm run build:kb 2>/dev/null || true'
```
---
**[inline/other] × 1** in: AppealArmor

```
/bin/bash -c 'INPUT=$(cat); CUSTOM=$(echo "$INPUT" | jq -r ".custom_instructions // \"\""); echo "🔄 PreCompact Guidance:"; echo "📋 IMPORTANT: Review CLAUDE.md in project root for:"; echo " • 54 available agents and concurrent usage patterns"; echo " • Swarm coordination strategies (hierarchical, mesh, adaptive)"; echo " • SPARC methodology workflows with batchtools optimization"; echo " • Critical concurrent execution rules (GOLDEN RULE: 1 MESSAGE = ALL OPERATIONS)"; if [ -n "$CUSTOM" ]; then echo "🎯 Custom compact instructions: $CUSTOM"; fi; echo "✅ Ready for compact operation"'
```
---
**[npx:aqe/agentic] × 1** in: AppealArmor

```
npx agentic-payments@latest mcp
```
---
**[inline/other] × 1** in: AppealArmor

```
aqe-mcp
```
---
**[script:exists] × 1** in: ugo-ai-register-now

```
/Users/stuartkerr/Code/ugo-ai-register-now/scripts/session-start-error-check.sh 2>/dev/null || true
```
---
**[script:exists] × 1** in: ugo-ai-register-now

```
/Users/stuartkerr/Code/ugo-ai-register-now/scripts/github-session-check.sh 2>/dev/null || true
```
---
**[npx:claude-flow(old)] × 1** in: ugo-ai-register-now

```
cmd=$(jq -r '.tool_input.command // empty' 2>/dev/null | head -c 800); if [ -n "$cmd" ]; then npx claude-flow@alpha hooks pre-command --command "$cmd" --validate-safety true --prepare-resources true >/dev/null || true; fi
```
---
**[inline/other] × 1** in: ugo-ai-register-now

```
cat | jq -r '.tool_input.command // empty' | grep -qE 'git push.*--no-verify|--no-verify.*git push' && echo '🚫 BLOCKED: --no-verify bypasses deployment checks! Use regular git push instead. If tests are failing, FIX THEM before deploying.' && exit 1 || true
```
---
**[script:exists] × 1** in: ugo-ai-register-now

```
cat | jq -r '.tool_input.command // empty' | grep -q 'git push' && /Users/stuartkerr/Code/ugo-ai-register-now/scripts/pre-deploy-check.sh || true
```
---
**[npx:claude-flow(old)] × 1** in: ugo-ai-register-now

```
f=$(jq -r '.tool_input.file_path // .tool_input.path // empty' 2>/dev/null | head -c 500); if [ -n "$f" ]; then npx claude-flow@alpha hooks pre-edit --file "$f" --auto-assign-agents true --load-context true >/dev/null 2>&1 || true; fi
```
---
**[script:exists] × 1** in: ugo-ai-register-now

```
cat | /Users/stuartkerr/Code/ugo-ai-register-now/scripts/database-architecture-check.sh 2>/dev/null || true
```
---
**[npx:claude-flow(old)] × 1** in: ugo-ai-register-now

```
cmd=$(jq -r '.tool_input.command // empty' 2>/dev/null | head -c 800); if [ -n "$cmd" ]; then npx claude-flow@alpha hooks post-command --command "$cmd" --track-metrics true --store-results true >/dev/null || true; fi
```
---
**[inline/other] × 1** in: ugo-ai-register-now

```
cat | jq -r '.tool_input.command // empty' | grep -qE 'git push|--no-verify' && echo '🚨 POST-PUSH VERIFICATION REQUIRED! You MUST: 1) gh run list --limit 1 (verify success), 2) curl -s -o /dev/null -w "%{http_code}" https://ugoai.org (verify 200), 3) npm run test:e2e:smoke:quick (verify tests pass). NEVER say deployed successfully until ALL verified!' || true
```
---
**[npx:claude-flow(old)] × 1** in: ugo-ai-register-now

```
f=$(jq -r '.tool_input.file_path // .tool_input.path // empty' 2>/dev/null | head -c 500); if [ -n "$f" ]; then npx claude-flow@alpha hooks post-edit --file "$f" --format true --update-memory true >/dev/null 2>&1 || true; fi
```
---
**[inline/other] × 1** in: PowerPlatePulse

```
sh -c 'F=$(jq -r ".tool_input.file_path // empty" 2>/dev/null); [ -n "$F" ] && /Users/stuartkerr/.npm-global/bin/ruflo hooks post-edit -f "$F" </dev/null >/dev/null 2>&1 &'
```
---
**[inline/other] × 1** in: PowerPlatePulse

```
sh -c 'C=$(jq -r ".tool_input.command // empty" 2>/dev/null | head -c 500); [ -n "$C" ] && /Users/stuartkerr/.npm-global/bin/ruflo hooks post-bash -c "$C" </dev/null >/dev/null 2>&1 &'
```
---
**[inline/other] × 1** in: PowerPlatePulse

```
sh -c 'P=$(jq -r ".prompt // empty" 2>/dev/null | head -c 1000); [ -n "$P" ] && /Users/stuartkerr/.npm-global/bin/ruflo hooks route -t "$P" </dev/null >/dev/null 2>&1 &'
```
---
**[inline/other] × 1** in: PowerPlatePulse

```
sh -c 'exit 0 # auto-memory-hook import disabled 2026-04-27 audit r2 — @claude-flow/memory unreachable from project, was emitting noisy SessionStart warning. Original in settings.json.pre-tier1.2.bak'
```
---
**[local-helper] × 1** in: PowerPlatePulse

```
node -e "var c=require('child_process'),p=require('path'),u=require('url'),r;try{r=c.execSync('git rev-parse --show-toplevel',{encoding:'utf8'}).trim()}catch(e){r=process.cwd()}var f=p.join(r,'.claude/helpers/auto-memory-hook.mjs');import(u.pathToFileURL(f).href)" sync
```
---
**[inline/other] × 1** in: PowerPlatePulse

```
sh -c 'exit 0 # auto-memory-hook sync disabled 2026-04-27 audit r2 — @claude-flow/memory unreachable from project, sync would have failed silently anyway. Original in settings.json.pre-tier1.2.bak'
```
---
**[inline/other] × 1** in: PowerPlatePulse

```
sh -c '/Users/stuartkerr/.npm-global/bin/ruflo hooks post-task -t "claude-code-turn" --success </dev/null >/dev/null 2>&1 &'
```
---
**[inline/other] × 1** in: PowerPlatePulse

```
echo '{"systemMessage": "🔴 RUFLO QA GATE: Stuart requires Ruflo QA before ANYTHING is marked done. Spawn Agent(subagent_type=feature-dev:code-reviewer) to review the last change. Do NOT report completion until the reviewer returns PASS."}'
```
---
**[npx:ruvector] × 1** in: PowerPlatePulse

```
npx ruvector mcp start
```
---
**[script:exists] × 1** in: PowerPlatePulse

```
node /Users/stuartkerr/Code/PowerPlatePulse/Docs/KB/ruview/kb-mcp-server.mjs
```
---
**[npx:@claude-flow] × 1** in: Site_Master

```
cat | jq -r '.tool_input.command // empty' | tr '\n' '\0' | xargs -0 -I {} npx @claude-flow/cli@latest hooks pre-command --command '{}' --validate-safety true --prepare-resources true
```
---
**[npx:@claude-flow] × 1** in: Site_Master

```
cat | jq -r '.tool_input.file_path // .tool_input.path // empty' | tr '\n' '\0' | xargs -0 -I {} npx @claude-flow/cli@latest hooks pre-edit --file '{}' --auto-assign-agents true --load-context true
```
---
**[npx:@claude-flow] × 1** in: Site_Master

```
cat | jq -r '.tool_input.command // empty' | tr '\n' '\0' | xargs -0 -I {} npx @claude-flow/cli@latest hooks post-command --command '{}' --track-metrics true --store-results true
```
---
**[npx:@claude-flow] × 1** in: Site_Master

```
cat | jq -r '.tool_input.file_path // .tool_input.path // empty' | tr '\n' '\0' | xargs -0 -I {} npx @claude-flow/cli@latest hooks post-edit --file '{}' --format true --update-memory true
```
---
**[script:DEAD] × 1** in: Site_Master

```
bash .claude/hooks/date-check.sh
```
---
**[npx:@claude-flow] × 1** in: Site_Master

```
npx @claude-flow/cli@latest hooks session-end --generate-summary true --persist-state true --export-metrics true
```
