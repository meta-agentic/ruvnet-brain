# 4.0 Readiness — derived state, not narrative

Updated: 2026-07-22 01:40:00 EDT | Version 1.0.0
Created: 2026-07-22 00:55:00 EDT

> **Why this file exists.** The owner asked for readiness four separate times on 2026-07-21/22 and got
> narrative each time. Narrative is how the gaps hid: a paragraph can be true sentence-by-sentence and
> still leave a reader believing a thing ships when it has zero call sites. This file is a table
> because a table has a column that a paragraph does not — **"Shipped to users?"** — and that column is
> where every one of tonight's gaps actually lives.
>
> **Trust rule for this document.** ✅ and ❌ appear ONLY where a command in §1 produced the evidence.
> Everything else says `unverified`, which is a real answer and is never a synonym for ❌. An
> optimistic readiness doc is worse than no readiness doc, because it costs a re-audit *plus* the
> trust. Where an ADR's own text disagrees with what the machine did, **the machine wins and the
> disagreement is recorded** — see §5.

---

## 1. Evidence base — what was actually run

Every ✅/❌ below traces to one of these. Run at 2026-07-22 00:17–01:40 EDT, in
`/Users/stuartkerr/Code/ruvnet-brain`, at HEAD `8a505a6`.

| # | Command | Result | Status |
|---|---|---|---|
| E1 | `npx vitest run` at 00:17 | 77 files passed, 14 skipped (91); **795 tests passed, 224 todo** (1019); 14.41s | ✅ green — **superseded by E11** |
| E2 | `npm test` (`plugin/test/run-tests.mjs`) — **cold**, `KB_MODEL_CACHE` unset | completed. Sections 1–3 pass; **capability battery returned `(no hit)` on every question** | ❌ **misconfigured run** |
| E2b | `npm test` — **warm**, `KB_MODEL_CACHE` set, `pipefail` on | **60/60 checks passed, 0 failures.** MCP launcher serves all **69 repos**; all 9 capability-battery questions return grounded hits (relevance 4.368–11.394) | ✅ **green** |
| E3 | `node scripts/capability-audit.mjs` | exit 0 — **3 dormant capabilities**, each with real evidence | ✅ runs |
| E4 | `node scripts/lesson-promote.mjs` | exit 0 — **736 lessons / 41 projects / 284 feedback / 7 promotable**, each citing projects | ✅ runs |
| E5 | `ruflo hooks list` | **26 hooks, 0 enabled, all "Never" run** — independently confirms E3's third finding | ✅ measured |
| E6 | `loadLessons()` from `scripts/lesson-store.mjs` | **12 lessons, all `status: candidate`**; enforcement `checklist`×10, `review`×2; **0 blocking**. Store: `~/.config/ruvnet-brain/lessons.json` | ✅ measured |
| E7 | `git log --oneline -15` | 5 commits tonight for 4.0 work, HEAD `8a505a6`; `origin/main..HEAD` is **empty** (pushed) | ✅ measured |
| E8 | `gh release view` / `gh release list` / `npm view` | **npm `latest` = 3.5.1-dev** (published 2026-07-22T04:17Z); **newest GitHub release = v3.4.22-dev** (2026-07-21); newest git tag = v3.4.22-dev | ❌ **drift** |
| E9 | `grep -rn "capability-audit"` across repo | **zero call sites** outside its own file, its ADR, and docs | ❌ not wired |
| E10 | `gh issue list` / `gh pr list` | **0 open issues, 0 open PRs**; #36 and #37 closed 2026-07-22 | ✅ clear |
| E11 | `npx vitest run` re-run at 01:40, after parallel-swarm edits landed in the working tree | **1 failed / 885 passed / 224 todo (1110).** `narrative-version.test.mjs`: *"explainer/index.html still claims 'What's new in 4.0' while 3.5.1-dev is shipping"* | ❌ **red** |

**On E1 — "green" covers 78% of the declared suite.** 224 of 1019 vitest cases report `todo`, i.e.
declared but not executed. The top concentrations are `prove-classification` (24), `build-symbols-
extraction` (20), `forge-ask-routing` (18), `forge-build-chunking` (18) and `behavioral-l1-l4-levels`
(13). **The cause is not established**: the files reporting the most `todo` cases contain no literal
`.todo()` calls, so this is not simply placeholder tests, and this document will not guess further.
Recorded because "795 passed" reads as total coverage and is not — and because `prove-classification`
and `behavioral-l1-l4-levels` are exactly the suites a readiness claim leans on. **Worth one hour
before 4.0; not a blocker on its own.**

**On E2 — two measurement errors of my own, recorded because they are the exact failures this repo
gates against.** Both were caught by re-checking rather than by reasoning, which is the only way they
ever get caught.

1. **I read an exit code that was not the suite's.** The first run was piped (`npm test 2>&1 | tail -8`)
   without `pipefail`, so bash reported `tail`'s status — `0` — while the runner's own last line is
   `process.exit(failures.length ? 1 : 0)` and it had **failed**. A green exit code that belonged to a
   different process. This is ADR-030's decision point #3 — *"did you re-check the artifact, or repeat
   what you were handed?"* — committed inside the document arguing that gate must exist.
2. **I nearly reported a broken brain.** The cold run's `(no hit)` on every battery question looks
   exactly like a retrieval outage. It was not: `KB_MODEL_CACHE` was unset, and the suite's own header
   warns about the first-run model download. With the cache warm, the same first question returns
   `concepts @ 9.324`. **The harness was misconfigured; the brain was fine.** Had this document been
   filed at the 35-minute mark it would have reported a P0 that does not exist.

**Resolution: E2b is green, 60/60.** All **8 battery questions observed failing cold** return grounded
hits warm — RuVector→`concepts @ 10.232`, AgentDB→`agentdb @ 8.429`, agentic-flow→`agentic-flow @
4.368`, plus SPARC, QuDAG, RuLake, SAFLA and metaharness. (8, not 9: my `tail -8` truncated the cold
log, so the ruflo question's cold result was never observed. Stating "all 9 failed" would be inventing
one data point to make a cleaner sentence.) **Both suites are green and the standing order is met.**

**On E11 — the suite went red while this document was being written, and the failure is this document's
entire thesis in miniature.** `explainer/index.html` was edited (uncommitted, parallel swarm) to say
**"What's new in 4.0"**. The product ships 3.5.x. This report grades it **L2 of L5**, with L3, L4 and
L5 all ❌ and **zero of the five acceptance metrics instrumented**.

The public page announced 4.0 before a single one of its defining levels was built — and the thing that
caught it was a **gate**, not a person and not a review. That is ADR-030's central measurement
(*"Gates: 8/8. Prose: 0/6"*) reproducing itself live, unprompted, inside the session arguing for it.

Two consequences, and neither is optional:

- **E1's green is superseded by E11's red.** The 795-passing run predates these edits. **`npm run
  version:check` also passes**, because the narrative claim lives in HTML prose it does not inspect —
  a second gate blind to a real drift, exactly like E8.
- **The fix is not to change the test.** Either the explainer reverts to 3.5, or 4.0 genuinely ships
  per §6. Editing the assertion to accept `4.0` would convert the one control that works into
  decoration. **Not fixed here — `explainer/index.html` is outside this agent's ownership.** Flagged
  for whoever owns it, as a release blocker.

**The real defect E2 exposed is the cold-run behaviour, and it is a live product risk.** With a cold
model cache the suite does not say "model cache cold" — it says `(no hit)` on every question, which is
*visually identical to the brain being empty or broken*. Anyone running `npm test` on a fresh machine
(the empty-first case this repo cares about most) will conclude the product is broken when it is fine.
A gate that fails misleadingly is worse than one that fails loudly.

---

## 2. ADR-028's L0–L5 proactivity ladder

ADR-028 §"Decision" defines 4.0 as **L3 + L4 + L5 shipped and measured**. Not L2 polished.

| Level | Built? | Tested? | Shipped to users? | Evidence |
|---|---|---|---|---|
| **L0 Reactive** — answers when asked | ✅ | ✅ | ✅ | **E2b: 60/60, all 9 battery questions return a grounded, cited hit from the expected repo.** L0's falsifiable test passes end-to-end against the real corpus, not mocks. The only level with all five ADR-028 test classes plausibly satisfiable today |
| **L1 Observant** — detects + displays state | ✅ | ✅ | ✅ | E3 renders 3 real faults with per-fault evidence; `console/app.js` renders machine state |
| **L2 Advocating** — detect → recommend → execute → reverse | ✅ | ⚠️ partial | ✅ | Advocacy **now renders** (`addRecommendations()` called from state/health/stack paths, `console/app.js:2288/2375/2400`, commit `7b8294f`). Remedy Registry closure test exists. The repair→distill→undo proof (1250 rows → 507 patterns → 0) is **ADR-027's claim, not re-verified here** |
| **L3 Contextual** — speaks in-session, no navigation | ❌ | ❌ | ❌ | **E9 is the whole answer.** `capability-audit.mjs` has zero call sites. The console is a page you must visit — ADR-028's exact definition of a PULL surface. `plugin/hooks/hooks.json` wires `ground-ruvnet`/`hijack-ruvnet`/`route-dispatch`/`verify-interface`/`design-wall` — **none of them advocate** |
| **L4 Anticipatory** — infers goal, names the better path first | ❌ | ❌ | ❌ | No goal-inference surface found in `scripts/` or `plugin/hooks/`. Nothing implements ADR-028's test ("names that path before the user commits to the worse one") |
| **L5 Compounding** — validated lessons promoted globally | ⚠️ miner only | ⚠️ 14 tests | ❌ | E4 mines correctly. But E6: **12 lessons, all `candidate`, 0 in force.** Promotion requires manual `--apply`. Survival-across-`--update` never demonstrated |

**Ladder grade: L2.** Unchanged from v3.5.0-dev. **L3, L4 and L5 are all ❌, and 4.0 is defined as all
three.** The single most load-bearing line in this document.

> **Note on a naming collision that will mislead a reader.** `README.md:382` publishes an **"L1–L4
> behavioral harness — all pass."** That is a *different ladder* (routing / deep-recall / implement /
> orchestrate) and has nothing to do with ADR-028's proactivity levels. Anyone reading the README
> today will reasonably conclude L4 proactivity ships. It does not. ADR-028 verification #1 requires
> the *proactivity* grade be published in the README; it is not there, and the existing L1–L4 line
> actively works against it.

---

## 3. ADR Verification items — verbatim, re-checked against the machine

Items are quoted from each ADR's own "Verification (what must be true before this is Accepted)"
section. The ✅/❌ marks in the **ADR's own text** are shown where present, so drift between what an
ADR claims and what the machine says is visible rather than smoothed over.

### ADR-027 — Capability advocacy and active signals (status: **Proposed**)

| # | Verification item (verbatim) | Built? | Tested? | Shipped? | Evidence |
|---|---|---|---|---|---|
| 1 | "A corrupt store produces a recommendation with a working one-click repair — proven on a real store, with row counts before and after." | ✅ | ⚠️ | ✅ | ADR body claims 1250 rows → 507 patterns/507 episodes/495 causal edges → undo → 0. **Not re-run this session** — accepted as the ADR's claim, not as verified state |
| 2 | "A machine with a healthy store and a live learner produces **no** recommendations (no false alarms)." | ❌ | ❌ | ❌ | Requires a healthy fixture machine. **No fixture exists** (§4). False-alarm rate is ADR-028's one non-negotiable 0 target and is currently **unmeasurable** |
| 3 | "A dormant capability on a real machine produces a real recommendation naming it." | ✅ | ✅ | ⚠️ | **E3 satisfies this outright** — 3 dormant capabilities named with evidence. Shipped only to the console (pull); E9 means nothing surfaces it unprompted |
| 4 | "A score drop from a persisted baseline fires an alarm, demonstrated on known-bad input." | ❌ | ❌ | ❌ | No persisted advocacy baseline found. ADR's own text: "not built — there is still no persisted baseline, so a cliff and a drift remain indistinguishable" |
| 5 | "The adversarial review of this ADR and its DDD is recorded, including anything it defeated." | ❌ | — | ❌ | `docs/reviews/` contains **exactly one file**, `0023-gpt56-redteam.md`. No ADR-027 review recorded |

**ADR-027's "Implementation status" section is STALE and must be corrected.** It states: *"Recommendations
are not rendered anywhere… The thesis is unshipped. Verification items 2, 3 and 4 below cannot pass until
this changes."* That was true when written. Commit `7b8294f` ("the brain speaks first — advocacy renders")
landed the render path, and `console/app.js:2371-2375` now calls `addRecommendations()` on the health
source. **Item 3 passes today.** Leaving the ADR uncorrected means the project's own record understates
what shipped — the mirror image of the usual failure, and equally a defect.

### ADR-028 — What "proactive" means (status: **Proposed**)

| # | Verification item (verbatim) | Built? | Tested? | Shipped? | Evidence |
|---|---|---|---|---|---|
| 1 | "The ladder is used to grade an actual release, and the grade is published in the README rather than asserted in a commit message." | ⚠️ | — | ❌ | **This document is the first grade** (§2). It is not in the README, and the README's unrelated "L1–L4 behavioral harness — all pass" line will be misread as this ladder |
| 2 | "A ground-truth fixture machine exists and produces a real number for recall and false-alarm rate." | ❌ | ❌ | ❌ | `find` for test fixtures returned **nothing**. ADR-028 itself calls this "the first build item, because without it every subsequent number is unfalsifiable" |
| 3 | "Latency-to-surface is instrumented and reported — the 21-day baseline is recorded, and a current value is measurable." | ❌ | ❌ | ❌ | No instrumentation for latency-to-surface found in `scripts/`, `console/`, or `tests/`. The 21-day baseline exists **as prose in the ADR**, not as a recorded datum |
| 4 | "An independent reader (not the author) grades the qualitative criterion." | ❌ | — | ❌ | Not done. Standing order: never grade your own work. **This document does not grade it either** |
| 5 | "This ADR and its DDD survive an adversarial cross-model review." | ❌ | — | ❌ | Not recorded (see ADR-027 #5) |

**None of ADR-028's five metrics (precision, recall, latency-to-surface, false-alarm rate, compounding
rate) is instrumented.** Zero of five. Since ADR-028 states these *are* the acceptance criteria for
4.0, **4.0 cannot currently be graded at all**, independent of how much code exists.

### ADR-029 — Cross-project lesson promotion (status: **Proposed**)

| # | Verification item (verbatim) | ADR says | Built? | Tested? | Shipped? | Evidence |
|---|---|---|---|---|---|---|
| 1 | "Runs on a real machine and produces evidence-cited output — 736 lessons, 41 projects, 7 promotable processes, each naming the projects that taught it." | ✅ | ✅ | ✅ | ⚠️ CLI only | **E4 reproduces the exact numbers** — 736/41/284/7. The ADR's ✅ is confirmed independently |
| 2 | "Tested at all five classes ADR-028 requires… 14 tests, with the central guard proven to FAIL when the predicate is weakened to a raw lesson count." | ✅ | ✅ | ✅ | n/a | `tests/unit/lesson-promote.test.mjs` exists and is inside E1's green run |
| 3 | "A promoted lesson survives a nightly refresh and a `--update`." | ❌ | ❌ | ❌ | ❌ | Confirmed still ❌. Nothing promoted (E6: 0 in force), so survival is untestable until something is promoted |
| 4 | "The proactive loop closes: the console offers the promotion unprompted… rather than the user having to know this script exists." | ❌ | ❌ | ❌ | ❌ | Confirmed still ❌. `lesson-promote.mjs` is referenced only by its own test, docs, and the in-flight `capability-registry.mjs`. **"A CLI nobody would think to run" — the ADR's own words about itself** |
| 5 | "Adversarial cross-model review recorded." | ❌ | ❌ | — | ❌ | Not recorded |

### ADR-030 — How promoted knowledge changes behaviour (status: **Proposed**)

| # | Verification item (verbatim) | ADR says | Built? | Tested? | Shipped? | Evidence |
|---|---|---|---|---|---|---|
| 1 | "A promoted lesson demonstrably changes behaviour at its decision point — shown by the gate firing on a case it would previously have let through, proven against known-bad first." | ❌ | ❌ | ❌ | ❌ | **E6 is decisive: 12 lessons, all `candidate`, enforcement `checklist`/`review`, zero `block`.** Nothing can fire. `lesson-store.mjs:42` defines the `ASSERT_FACT` decision point but **no hook reads it** |
| 2 | "Measured context cost of the constitution + per-gate retrieval is **below** today's baseline." | ❌ | ❌ | ❌ | ❌ | Not instrumented. ADR flags its own ~1,450-token figure as "a calculation, not a measurement, and must not be repeated as fact" — **honoured here: it is not repeated as fact** |
| 3 | "A promoted lesson survives `npx ruvnet-brain --update` and a nightly refresh." | ❌ | ❌ | ❌ | ❌ | Shared with ADR-029 #3. Untestable while 0 are promoted |
| 4 | "The management surface lists every promoted lesson with provenance, and demotion is sticky across a subsequent mining run." | ❌ | ❌ | ❌ | ❌ | No such surface found in `console/`. §4 (top-ten page) is the nearest ask and it does not exist |
| 5 | "Adversarial cross-model review recorded." | ❌ | ❌ | — | ❌ | Not recorded for 027/028/029/030 |

**ADR-030 gates 1–3 (the assumption gates) do not exist.** The ADR names these as the highest-frequency
failure — *"Gates 4–7 exist today. 1–3 do not, and they are the ones that fail most often."* Confirmed:
`plugin/hooks/hooks.json` wires `ground-ruvnet`, `hijack-ruvnet`, `route-dispatch`, `verify-interface`,
`design-wall`. **None fires on "before asserting a fact", "before recommending an architecture", or
"before relaying a number."**

### ADR-031 — The compounding brain (status: **Proposed**)

| # | Verification item (verbatim) | ADR says | Built? | Tested? | Shipped? | Evidence |
|---|---|---|---|---|---|---|
| 1 | "The offensive capability audit runs and surfaces at least one real dormant capability the defensive gate never would have — with the `.metaharness` finding as the ground-truth first case." | ❌ | ✅ | ✅ | ❌ | **This ADR's ❌ is now out of date on the first two columns.** E3 surfaces the `.metaharness` case exactly as specified: 0.285 → 0.765, 7 surfaces explored, **0 of 15 improvements kept, idle 13 days**. It is **not shipped** — E9, zero call sites |
| 2 | "A benchmark suite exists whose baseline `taskSuccess` is non-zero." | ❌ | ❌ | ❌ | ❌ | Confirmed ❌. E3 reports every variant plateaued at the same score — the documented consequence of a weak benchmark |
| 3 | "`evolve --bench` promotes at least one variant above 0.765 under the graded gate, reproducibly." | ❌ | ❌ | ❌ | ❌ | Blocked on #2. ADR-031 explicitly defers this ("doing it badly is worse than not doing it") |
| 4 | "A promoted lesson measurably changes a Darwin score." | ❌ | ❌ | ❌ | ❌ | Blocked on ADR-030 #1 and E6 |
| 5 | "The user corpus survives `--update` and a nightly refresh." | ❌ | ❌ | ❌ | ❌ | Shared with ADR-029 #3 / ADR-030 #3. **Three ADRs depend on this one unproven property** |
| 6 | "Adversarial cross-model review recorded for this ADR and its DDD." | ❌ | ⚠️ **ran, not recorded** | — | ❌ | **A real review happened.** Commit `8a505a6`: GPT-5.6-Sol hostile review of ADR-031/DDD-0005, **7 findings, 3 CRITICAL, all fixed in code** (trust boundary on lesson creation; objective-function contamination). But `docs/reviews/` holds only `0023-gpt56-redteam.md` — **the review exists solely as a commit message.** The standing order says the outcome lives *in* the record. Half-done, and the half that is missing is the auditable half |

---

## 4. The nine things the owner asked for

| # | Ask | Built? | Tested? | Shipped to users? | Evidence |
|---|---|---|---|---|---|
| 1 | **Proactive advocacy** | ⚠️ partial | ⚠️ | ❌ **pull only** | Renders in console (`7b8294f`, `app.js:2371-2375`) = L2. **E9: `capability-audit.mjs` has zero call sites**, so nothing speaks unprompted. This is the L3 gap and it is *the* 4.0 gap |
| 2 | **Console clarity** | ⚠️ in flight | ❌ | ❌ | `console/app.js`, `index.html`, `style.css`, `tips.html` all **modified and uncommitted** by a parallel swarm at time of writing. Cannot be graded mid-edit; not in E1's committed state |
| 3 | **Top-ten page** | ❌ | ❌ | ❌ | **Does not exist.** No file matching top-ten/top10 in `console/` or `explainer/`; only hit repo-wide is an unrelated identifier in `scripts/corpus-qa.mjs` |
| 4 | **Tips rewrite** | ❌ | ❌ | ❌ | `console/tips.html` last committed **2026-07-18**, i.e. predates tonight entirely. Uncommitted edits in flight (parallel swarm) |
| 5 | **Admin page** | ⚠️ | unverified | ⚠️ | `explainer/admin.html` + `explainer/api/admin-stats.mjs` **exist and predate tonight** (the traffic-telemetry work). Whether this is the admin page being asked for is **unverified** — no ADR scopes it |
| 6 | **Learning turned on** | ❌ | ✅ measured | ❌ | **E5 and E3 agree independently: 26 hooks registered, 0 enabled, all "Never" run.** Learning is off. This is ADR-027's own North Star case, still open in this repo |
| 7 | **Cross-project lessons** | ✅ miner | ✅ 14 tests | ❌ | E4 mines 7 promotable processes with citations. **E6: 0 promoted, 0 in force.** Requires manual `--apply`; console never offers it (ADR-029 #4) |
| 8 | **Issues / PRs** | ✅ | ✅ | ✅ | **E10: 0 open issues, 0 open PRs.** #36 and #37 closed 2026-07-22. The only unambiguous ✅ in this table |
| 9 | **Versioning** | ❌ | ✅ measured | ❌ **drift** | **E8: npm `latest` is a full minor ahead of the newest GitHub release.** npm published 2026-07-22T04:17Z; newest release + newest git tag are both from 2026-07-21, one minor behind. Plugin auto-update consumes the **release** bundle — so plugin users are **not** getting tonight's advocacy work |

**On #9, stated plainly because it is the one that silently misleads users.** Two channels disagree.
Anyone who `npm install`s gets tonight's code; anyone on the plugin auto-update path gets last night's.
Both will report a version confidently, and only one is current. This is the exact shape of the failure
the "walk every channel before shipped" standing order was written for, and it is live right now.

**And the gate that exists cannot see it.** `npm run version:check` was run against this working tree
and passes: *"all surfaces agree ✓"*. It is telling the truth about what it inspects — **files inside
the repo** — and is structurally blind to the two surfaces users actually install from. A green gate
next to a real drift is worse than no gate, because it converts "nobody checked" into "we checked and
it was fine." **The drift in ask #9 was found by hand, not by the gate, and the gate should be
extended to compare `npm view` and `gh release view` against `plugin.json` before 4.0.**

---

## 5. Where the ADRs disagree with the machine

Recorded because a readiness doc that silently "fixes" its sources is just narrative with a table
around it. In all four cases the machine was checked and the ADR was not re-read against it.

| Claim | Source says | Machine says | Direction |
|---|---|---|---|
| Advocacy rendering | ADR-027: "Recommendations are not rendered anywhere… the thesis is unshipped" | `console/app.js:2288/2375/2400` call `addRecommendations()`; commit `7b8294f` | ADR **understates** — item 3 passes |
| Offensive audit | ADR-031 #1: ❌ | E3 runs and surfaces the `.metaharness` ground-truth case exactly as specified | ADR **understates** build/test; **shipped is still ❌** |
| ADR-031 adversarial review | ADR-031 #6: ❌ outstanding | Review ran (GPT-5.6-Sol, 7 findings, 3 CRITICAL, fixed in `8a505a6`); never written to `docs/reviews/` | **Ran but unrecorded** — ADR right for the wrong reason |
| Capability registry | DDD-0006: "No Capability registry exists… zero call sites" | `scripts/capability-registry.mjs` exists, uncommitted, parallel swarm | DDD **stale within hours**; still zero *committed* call sites, so its conclusion holds |

---

## 6. What must happen for an honest 4.0 — shortest ordered list

**0. Get the suite green again — today, before any of the below** (E11). `explainer/index.html` claims
"What's new in 4.0" against a 3.5.x product, and `narrative-version.test.mjs` is red because of it.
Revert the explainer to 3.5 until 4.0 actually ships. **It is item 0 rather than item 1 because it is
not 4.0 work at all** — it is a live public page making a claim the rest of this document disproves,
and it is the cheapest fix on the list. Owned by the parallel swarm; not fixed here.

Ordered by dependency, not by effort. Each item unblocks the ones below it; nothing here is optional
and nothing here is padding.

1. **Make `npm test` fail honestly on a cold model cache** (E2/E2b). Both suites pass on the committed
   tree, so this is not a blocker — it is first because it is an hour's work and it currently mislabels
   a healthy brain as a broken one. On a fresh machine the battery prints `(no hit)` on every question
   (8 observed), indistinguishable from a retrieval outage. Detect the cold cache and say so, or default
   `KB_MODEL_CACHE`. **Empty-first is a house rule, and this is the one place the test surface violates
   it.**

2. **Reconcile the release channels** (E8, ask #9). Tag and cut a GitHub release matching what npm
   already has. Plugin users are a full minor behind *right now*; every claim made to them is wrong
   until this lands.

3. **Build the ground-truth fixture machine** (ADR-028 #2). ADR-028 names it "the first build item,
   because without it every subsequent number is unfalsifiable." Recall and false-alarm rate — the
   latter a non-negotiable 0 — are unmeasurable without it, and **0 of the 5 acceptance metrics are
   instrumented today.** No fixture, no grade; no grade, no 4.0.

4. **Wire the offensive audit to an in-session surface** (E9 → L3). One consumer for
   `capability-audit.mjs` that speaks without the user opening a page, with a one-action permanent
   silence. This single change moves the ladder L2 → L3 and is the largest value-per-line in the list.

5. **Instrument the five metrics** (ADR-028 #3), latency-to-surface first. The 21-day baseline is
   currently prose; it must become a recorded datum with a current value beside it.

6. **Turn learning on** (E5, ask #6): 26 hooks, 0 enabled. This is ADR-027's own North Star case, still
   open in the repo that wrote it. It needs a remedy with a real undo, not a documentation line.

7. **Ratify at least one lesson and prove it fires** (E6, ADR-030 #1). 12 candidates, 0 in force. Show a
   gate blocking a case it previously let through, proven against known-bad first — per the trust
   boundary established in `8a505a6`, only `origin: user-stated` may reach `block`.

8. **Prove survival across `--update` and a nightly refresh** (ADR-029 #3 = ADR-030 #3 = ADR-031 #5).
   **One property, three ADRs.** Unprovable until #7 promotes something; highest leverage of any single
   remaining test.

9. **Record the adversarial reviews** (ADR-027/028/029/030 #5, ADR-031 #6). ADR-031's already **ran** —
   write it to `docs/reviews/` including what it defeated. Run the other four. No ADR may move to
   Accepted before this; it is the project's own standing order.

10. **Publish the ladder grade in the README** (ADR-028 #1) and **disambiguate the existing "L1–L4
    behavioral harness" line**, which today reads as an L4 proactivity claim to anyone who has not read
    ADR-028.

11. **Build ADR-030 gates 1–3** — assert-fact, architecture, relayed-number. The ADR's own measurement
    was **gates 8/8 complied, prose 0/6**, and these three cover the highest-frequency failure class.
    Last only because it is the largest, not because it is the least important.

**Then, and only then, grade against ADR-028's five metrics on the fixture machine and let an
independent reader grade the qualitative criterion.** Items 3, 5 and 9 exist so that this final step is
a measurement rather than an opinion — which is the difference between a 4.0 and a 3.5 with a bigger
number on it.

### Not covered by this document

Stated explicitly, because a readiness doc that hides its own blind spots is the failure it exists to
prevent:

- **Console clarity, tips rewrite, and the capability registry** (asks #2, #4) were **mid-edit and
  uncommitted** by a parallel swarm while this ran. They are ungradeable, not failing.
- **ADR-027 item 1** (corrupt-store repair, 1250 rows → 507 patterns → undo) was **not re-run**. It is
  reported as the ADR's claim, not as verified state.
- **The qualitative criterion is not graded here.** This document was written by an agent inside the
  project; per the standing order, it cannot grade its own work, and calling this an independent grade
  would be the exact inflation it was commissioned to end.
