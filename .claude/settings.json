{
  "_note": "Project-scoped config for ruvnet-brain ONLY. version-bump-gate enforces THIS repo's every-commit-bumps rule (Stuart, 2026-07-13); it lived in the GLOBAL settings until 2026-07-14, taxing every Bash command in all 34+ projects with a rule that is only true here. A hook belongs in the narrowest scope where it is true.",
  "env": {
    "RUFLO_HARNESS_LOOP": "1"
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "/bin/bash \"/Users/stuartkerr/Code/ruvnet-brain/plugin/scripts/version-bump-gate.sh\"",
            "timeout": 5
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "_note": "Runs THIS repo's FIXED continuation-gate (ADR-043) directly, so re-engagement is live here NOW instead of waiting on the spine flip (installed plugin is still 3.9.18, the pre-fix copy). Harmlessly redundant with the plugin's own Stop hook — the fixed gate's cooldown + shared ledger prevent double-forcing. REMOVE this override once the spine ships >=3.9.34, when the plugin copy is itself the fixed one.",
            "type": "command",
            "command": "node \"/Users/stuartkerr/Code/ruvnet-brain/plugin/scripts/continuation-gate.mjs\" || true",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
