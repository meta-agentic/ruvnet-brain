---
description: "RvCB — RuvNet Brain Console (same console as /rvbc, /brain-console, and /ruvnet-brain:configure — every spelling works). Opens the live console page: your whole RuvNet stack on one page. Read-only until you click."
---

Alias of `/rvbc`. **Follow the instructions in `rvbc.md` in this same directory exactly** — one short
sentence first ("opening it now; it scans live while you watch"), then the server in the BACKGROUND,
then the URL. Never promise a duration: the page carries its own timing.

Never tell the user they typed the wrong name — `/rvbc`, `/rvcb`, `/brain-console` and
`/ruvnet-brain:configure` are all correct and all land here.
